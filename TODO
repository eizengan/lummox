- window resize, border, fullscreen revamp
  - allowed to change when fullscreen?
  - return whether changed instead of raising error?
  - other flag methods?
- use helper for flags-type enums
  - message box flags
  - message box button flags
  - audio spec format flags
  - renderer flags
  - renderer flip
  - keymod?
  - joystick hat?
- explore scancode/keycode/name when scancode/keycode are not in-sync
  - safe to add KeyboardEvent#name?
  - safe to add Keyboard#pressed?(name)?
- display modes by index - what happens after disconnect, reconnect?
  - also mice
- figure out mouse state button macros https://wiki.libsdl.org/SDL_GetMouseState#remarks
- testing
  - fix existing tests
  - strategize tests for FFI wrapper
  - strategize tests for API
